export const SOLANA_ALL_BALANCES_PROMPT = `Get all of the balances of a given Solana wallet.

After showing the balance cards, do NOT list balances again in text. Respond only with:
"Balances shown above. Pick a token to swap, lend, stake, or explore next."`;
